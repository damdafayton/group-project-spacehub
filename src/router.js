import React from 'react'

function router() {
    return (
        <Router>
            <Route exact path='/' element={ } />
            <Route path='/mission' element={ } />
        </Router>
    )
}

export default router